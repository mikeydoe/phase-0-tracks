var colors = ["Blue", "Turquoise", "Black", "Scarlet"];

var names = ["Jeffrey", "Bruce", "Jim", "Sweeney"];


Map<String, String> horses = new HashMap<>();
for (int i = 0; i < keys.length; i++) {
   map.put(names[i], colors[i]);
}